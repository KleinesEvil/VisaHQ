export interface IOrders {
	/* 
		Here should be description of orders.json's structure in order to avoid errors. Just like in agents.ts interface. 
		But for this little test task its not so critical, imho (and it is realy big tbh xD). 
	*/
}